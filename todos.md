- each project folder should have it's own code history.
- We need to create sessions for the agent coding window so I can create new sessions and return to old ones.
- add a home button next to the back button that takes you to the overview page.

- codeswim currently has:
Pick a folder containing markdown files with embedded mermaid diagrams. The agent in the right panel will edit those diagrams first, then code at the leaves.

File → Open Folder… (⌘O)

but we also need a way to create a new project. It should create a new empty project folder that they can work in. 

Also, it would be nice to have an recent list like vscode does.
