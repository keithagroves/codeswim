node_modules
dist
out
.turbo
.DS_Store
.eslintcache
*.log*
.playwright-cli/
pitch/
.env
.env.*
!.env.example
.wrangler/

# Personal, local-only Agents-view tab state (opencode session ids) — not
# project content, would just be merge-conflict noise if shared.
.codeswim/agent-tabs.json

# Added by codeswim sync
flyer-web/
.archive/

# Added by codeswim sync
.codeswim/agent-state.json
